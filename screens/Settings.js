import { StyleSheet, Text } from "react-native";

function Settings() {
	return <Text style={styles.container}>تنظیمات</Text>;
}
export default Settings;

const styles = StyleSheet.create({
	container: {
		flex: 1,
	},
});
